# Implementation Framework

## Summary

* Incremental adoption model: Supporting publishers to start with unstructured documents, and move on to provide structured and ultimately linked data.
* Publication levels: Separating fields into 'basic', 'intermediate' and 'advanced' so that publishers can focus on a small set first.
* Recommended fields: A list of fields that publishers are encouraged (but not required) to provide.
